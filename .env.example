# === Osaifill Environment Settings ===

# --- Backend Settings ---
# Local (Manual): sqlite:///./osaifill.db (within backend directory)
# Docker: sqlite:////app/data/osaifill.db
DATABASE_URL=sqlite:///./osaifill.db
CORS_ALLOW_ORIGINS=http://localhost:5173

# --- Frontend Settings ---
# If you are using Vite proxy (local development), keep it as /api
# If you are calling backend directly, use http://localhost:8000/api
# For Docker production, /api is recommended (proxied by Nginx)
VITE_API_BASE_URL=/api
VITE_HIDE_UNASSIGNED_BANNER=false

# --- Development Settings (Vite Proxy) ---
# Backend URL for Vite proxy during 'npm run dev'
VITE_BACKEND_URL=http://localhost:8000
